name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    name: Lint & format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Install dev deps
        run: |
          uv pip install --system setuptools
          uv pip install --system -e ".[dev]" --no-build-isolation
      - run: ruff check .
      - run: ruff format --check .

  typecheck:
    name: Type check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Install dev deps
        run: |
          uv pip install --system setuptools
          uv pip install --system -e ".[dev]" --no-build-isolation
      - run: pyright buildstamp/

  test:
    name: Test / Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Install (Python ${{ matrix.python-version }})
        run: |
          uv pip install --system setuptools --python ${{ matrix.python-version }}
          uv pip install --system -e ".[dev]" --no-build-isolation --python ${{ matrix.python-version }}
      - name: Run tests
        run: uv run --system --python ${{ matrix.python-version }} pytest -v

  build:
    name: Build distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Build sdist + wheel
        run: |
          uv pip install --system setuptools
          uv pip install --system -e . --no-build-isolation
          uv build --no-build-isolation
      - name: Check dist contents
        run: |
          uv pip install --system twine
          twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7
